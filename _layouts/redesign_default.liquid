<!doctype html>
<html lang="{{ site.lang }}">
  <head>
    {% include head.liquid %}
  </head>
  <body class="{% if page.permalink == '/' %}home{% endif %}">
    <div class="ink-bg" aria-hidden="true"></div>
    <div class="shell">
      {% include redesign_nav.liquid %}
      {{ content }}
      {% include redesign_footer.liquid %}
    </div>
    {% include scripts.liquid %}
  </body>
</html>
